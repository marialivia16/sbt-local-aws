import org.scalatest.{FlatSpec, Matchers}

class AwsServiceTest extends FlatSpec with Matchers {
  "AwsService" should "extract parameters from cf" in {
//    println(AwsService.DynamoDB.createJsonCommand(cfJson, Dictionary(Map("AppName" -> "Example", "Environment" -> "CODE"))))
  }
}